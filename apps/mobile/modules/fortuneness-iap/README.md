# Fortuneness IAP Expo module

The Swift/StoreKit 2 local Expo module belongs here after the Phase 0 Mac/Xcode and App Store Sandbox spike establishes the distinct trust paths and verified JWS delivery with `appAccountToken`. This boundary is intentionally documentation-only until that external gate is complete.
